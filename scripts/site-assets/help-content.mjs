// Curated, original per-module troubleshooting tips — no LLM, no external
// content, just the common blockers this course's own author has seen Vue
// developers hit at each stage. Keep entries short (3-4 bullets) and
// specific to what actually trips people up in that exact module.

export const focusResetTips = [
  'Отойдите от экрана на 5 минут — буквально физически. Мозг часто решает проблему в фоне, пока вы на неё не смотрите.',
  'Объясните баг вслух, будто рассказываете коллеге — вслух, а не в голове. Часто на середине объяснения становится ясно, что не так.',
  'Перечитайте раздел «Идея модуля» в README ещё раз. Если задача не поддаётся, обычно дело не в синтаксисе, а в том, что сама идея модуля ещё не до конца прижилась.',
];

export const helpContent = {
  '00-matching-terms': [
    'Ищете точный аналог `v-model`, `computed` или `watch`? Не пытайтесь найти замену 1:1 — большинство «примитивов» Vue в React разбиты на комбинацию из нескольких простых инструментов.',
    'Шпаргалка в этом модуле — не для заучивания наизусть, а чтобы быстро найти нужную строку, когда зависнете в конкретном модуле.',
    'Термин встретился впервые и пока не понятен — не блокируйтесь на нём здесь. Каждый следующий модуль объясняет свои термины по ходу дела.',
  ],
  assessment: [
    'Промахнулись в половине вопросов? Это и есть смысл диагностики — не оценка, а список модулей, которые точно нельзя пробегать по диагонали.',
    'Правильный ответ понятен, а вот почему неправильные варианты неправильные — нет? Перечитайте разбор ещё раз, ловушка обычно именно там.',
    'Диагностика не сохраняет результат между визитами — возвращайтесь к ней в любой момент, когда захотите перепроверить себя.',
  ],
  '01-components-jsx': [
    'JSX не компилируется? Проверьте: один корневой элемент на `return` (или `<>...</>`), `className` вместо `class`, `{}` для JS-выражений внутри разметки.',
    '`ReferenceError` с именем компонента почти всегда значит, что он не импортирован явно — в отличие от глобальных компонентов Vue, здесь любой компонент — обычный `import`.',
    'Пропс не доходит до компонента? Сверьте название атрибута в JSX с именем в деструктуризации пропсов — буква в букву.',
  ],
  '02-state-and-reactivity': [
    'Значение точно поменялось, а на экране — нет? Проверьте, не `useRef` ли это вместо `useState` — `.current` не планирует рендер.',
    'Мутировали массив/объект и вызвали `setState` тем же значением — экран не обновится: React сравнивает ссылку, а не содержимое.',
    '`count` в обработчике клика «залип» на старом значении — это устаревшее замыкание, разберём подробно в следующем модуле.',
  ],
  '03-use-effect': [
    'Эффект выполняется дважды при монтировании в dev-режиме — это `React.StrictMode`, намеренно, не баг. В проде — один раз.',
    'Забыли `cleanup` в эффекте с подпиской/таймером — при размонтировании компонента подписка продолжит жить и может дёргать уже удалённый компонент.',
    'Эффект «не видит» новых пропсов/state — проверьте массив зависимостей: скорее всего туда не попало то, что реально нужно отслеживать.',
  ],
  '04-composition': [
    'Кастомный хук не работает, если вызван условно или внутри цикла — те же правила хуков, что и для `useState`/`useEffect`.',
    'Один и тот же кусок логики повторяется в двух компонентах? Это повод вынести его в кастомный хук, а не копировать.',
    '`children` — обычный пропс. Если ничего не рендерится, проверьте, что вы правда передали что-то внутрь тега компонента, а не как отдельный атрибут.',
  ],
  '05-global-state': [
    'Бесконечный ре-рендер после подключения Redux почти всегда значит, что селектор возвращает новый объект/массив на каждый вызов — селектите примитивы или мемоизируйте.',
    '`state.someSlice` — `undefined`? Сверьте имя ключа в `configureStore` с тем, что вы читаете в селекторе.',
    'В Redux Toolkit `state.items.push(...)` внутри reducer — это не мутация в привычном смысле: Immer перехватывает такой код и строит новое состояние. Но только внутри reducer, больше нигде.',
  ],
  '06-routing': [
    'Модалка не закрывается по Esc? Проверьте, что обработчик клавиши висит на `document`, а не только на элементе модалки — у него может не быть фокуса.',
    'После F5 модалка превращается в обычную страницу — это правильное поведение паттерна background location, а не баг.',
    '`useNavigate` вызывается, а URL не меняется — убедитесь, что компонент действительно внутри `<BrowserRouter>`.',
  ],
  '07-forms-and-data': [
    'Инпут «не даёт печатать» — это контролируемый компонент без `onChange`. React возвращает значение назад на каждое нажатие, раз state не обновляется.',
    'Ошибка формы показывается сразу при загрузке, ещё до единого клика — проверьте, что флаг вроде `touched` не включён по умолчанию.',
    'Быстрый двойной клик отправляет запрос дважды — задизейбльте кнопку на время `pending`, это дешевле любой защиты на сервере.',
  ],
  '08-typescript': [
    '`Property does not exist on type...` в разветвлённом состоянии — забыли сузить дискриминированное объединение проверкой на `status` перед обращением к полю.',
    'Дженерик-параметр TypeScript почти всегда выводится сам из аргументов — если ошибка про несовпадение типов, сначала проверьте форму переданных данных, а не пишите тип руками.',
    '`any` — это не «типизировал», а «выключил проверку». Если тип совсем не понятен — `unknown` безопаснее и всё равно заставит сузить перед использованием.',
  ],
  '09-rendering-and-performance': [
    'Обернули компонент в `memo`, а перерисовки не прекратились? Проверьте, не передаёте ли вы функцию/объект инлайн в пропсах — тогда это всегда «новое» значение.',
    'Не оборачивайте вообще всё в `memo`/`useCallback` — у сравнения пропсов есть своя цена, и для дешёвых компонентов она может быть выше, чем просто рендер.',
    'Хотите увидеть лишний рендер своими глазами, а не гадать — посчитайте: `data-render-count` в этом модуле именно для этого.',
  ],
  '10-final-project': [
    'После входа `/profile` всё ещё редиректит на `/login`? Проверьте порядок: `dispatch(login(...))` должен отработать до `navigate`, а не после.',
    'Забыли `replace` в `<Navigate to="/login" replace />` — кнопка «назад» в браузере будет прыгать между защищённой страницей и логином.',
    'Все кусочки уже работали по отдельности в модулях 05–07 — если что-то не собирается, ищите разрыв именно на стыке между ними.',
  ],
  'practicum-map': [
    'Проходите официальную программу параллельно? Не пытайтесь синхронизировать оба курса построчно — это лишняя нагрузка. Сначала разберитесь в идее здесь своими словами, потом переносите в официальный проект.',
    'Названия и структура файлов в официальном ТЗ и в этом курсе отличаются намеренно — важна идея (модальные маршруты, дискриминированные состояния), а не конкретные имена файлов.',
  ],
};
