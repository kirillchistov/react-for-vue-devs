export function isValidEmail(email: string): boolean {
  // TODO: верните true, если email похож на настоящий — есть "@", хотя бы
  // один символ до него и после (включая точку в домене).
  //
  // Подсказка: регулярного выражения вида /^\S+@\S+\.\S+$/ более чем
  // достаточно для учебной формы — «настоящая» валидация email намного
  // более скользкая тема (и в реальности всё равно ничего не гарантирует
  // без письма-подтверждения), сюда мы не полезем.
  return false;
}

export function isValidPassword(password: string): boolean {
  // TODO: пароль валиден, если в нём хотя бы 6 символов.
  return false;
}
