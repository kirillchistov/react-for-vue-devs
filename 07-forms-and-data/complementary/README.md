# Дополнительный пример: форма обратной связи

Тот же принцип, что в LoginForm основного упражнения (контролируемые инпуты + валидация), но с валидацией по потере фокуса (`onBlur`), а не только по попытке отправить форму — распространённый вариант того же паттерна. Уже решено, TODO нет.

```sh
npm install   # один раз, из корня репозитория (npm workspaces)
npm run dev    # http://localhost:5173
```
