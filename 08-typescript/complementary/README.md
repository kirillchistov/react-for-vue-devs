# Дополнительный пример: generic-компонент

Основное упражнение показывало дженерик на хуке (`useLocalStorage<T>`). Здесь та же идея — но на компоненте: один `Select<TItem>` используется и со списком строк, и со списком объектов, и в обоих случаях `getLabel`/`onSelect` типизированы точно под переданный `items`, без единого `any`. Уже решено, TODO нет.

```sh
npm install   # один раз, из корня репозитория (npm workspaces)
npm run dev    # http://localhost:5173
```
